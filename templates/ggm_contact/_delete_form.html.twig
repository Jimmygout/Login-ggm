<form method="post" action="{{ path('ggm_contact_delete', {'pkGgmContact': ggm_contact.pkGgmContact}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ggm_contact.pkGgmContact) }}">
    <button class="btn">Delete</button>
</form>
